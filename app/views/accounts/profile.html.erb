<div class="container">
  <div class="row">
    <div class="col-4">
    </div>
    <div class="col-8 text-left">
      <h1><%= @account.full_name %></h1>
      <%= link_to "Edit Profile", edit_account_registration_path, class: "btn btn-secondary" %>
      <div class="profile-stats"><%= @account.posts.count %> Posts</div>
      <div class="profile-stats"><%= @account.total_followers %> Posts</div>
      <div class="profile-stats"><%= @account.total_following %> Posts</div> 
    </div>
  </div>
</div>